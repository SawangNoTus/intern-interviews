/**
 * ตรวจสอบว่าเลขที่รับมาเป็นจำนวนเฉพาะหรือไม่
 * @param {number} num - ตัวเลขที่ต้องการตรวจสอบ
 * @returns {boolean} - true ถ้าเป็นจำนวนเฉพาะ, false ถ้าไม่ใช่
 */
function isPrime(num) {
  // TODO: เขียนโค้ดที่นี่

}

module.exports = isPrime;
