def is_prime(num: int) -> bool:
    """
    ตรวจสอบว่าเลขที่รับมาเป็นจำนวนเฉพาะหรือไม่

    Args:
        num: ตัวเลขที่ต้องการตรวจสอบ

    Returns:
        True ถ้าเป็นจำนวนเฉพาะ, False ถ้าไม่ใช่
    """
    # TODO: เขียนโค้ดที่นี่
    pass
